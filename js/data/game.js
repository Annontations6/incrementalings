var game = {
  layers:[{
    startData:{
      unlocked:true,
      resource:new Decimal(0)
    },
    name:"Mass",
    layerId:0,
    layerShown() {return true},
  }],
  changelog:new Changelog("Changelog", changelog)
}
